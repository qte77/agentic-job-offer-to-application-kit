---
name: Deploy Pages

# Publishes the static ui/ dashboard. It ships synthetic demo data only (no PII),
# so it clears the ADR-0001 gate; wiring the live `data` branch stays gated on #52.
# No build step — the site is served as-is.
on:
  push:
    branches: [main]
    paths:
      - 'ui/**'
      - '.github/workflows/pages.yaml'
  workflow_dispatch:

permissions: {}

# Serialize deploys; don't cancel an in-flight one (GitHub Pages guidance).
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Build + deploy ui/
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
      contents: read
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.3
        with:
          persist-credentials: false

      - name: Configure Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b  # v5.0.0

      - name: Upload ui/ artifact  # no build — static site served as-is
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa  # v3.0.1
        with:
          path: ui

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e  # v4.0.5
...
